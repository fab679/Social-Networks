import { IExecutableSchemaDefinition } from "@graphql-tools/schema";
import { ITypeDefinitions, IResolvers } from "@graphql-tools/utils";
import { GraphQLSchema } from "graphql";
import { Node } from "../classes";
declare type SchemaDirectives = IExecutableSchemaDefinition["schemaDirectives"];
declare function makeAugmentedSchema({ typeDefs, resolvers, schemaDirectives, }: {
    typeDefs: ITypeDefinitions;
    resolvers?: IResolvers;
    schemaDirectives?: SchemaDirectives;
}): {
    schema: GraphQLSchema;
    nodes: Node[];
};
export default makeAugmentedSchema;
//# sourceMappingURL=make-augmented-schema.d.ts.map